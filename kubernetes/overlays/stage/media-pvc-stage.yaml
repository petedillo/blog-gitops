apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: blog-media-pvc-stage
  namespace: blog-stage
  labels:
    environment: stage
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1Ti
  selector:
    matchLabels:
      type: nfs
  storageClassName: ""