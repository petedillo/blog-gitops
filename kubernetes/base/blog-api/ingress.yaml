apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: blog-api
spec:
  rules:
  - host: api.dev.petedillo.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: blog-api
            port:
              number: 8080
