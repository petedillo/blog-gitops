apiVersion: v1
kind: Service
metadata:
  name: blog-api
  labels:
    app: blog-api
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: blog-api
