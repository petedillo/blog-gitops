apiVersion: v1
kind: Service
metadata:
  name: blog-agent
  namespace: blog-dev
  labels:
    app: blog-agent
spec:
  type: ClusterIP
  ports:
  - port: 3004
    targetPort: 3004
    protocol: TCP
    name: http
  selector:
    app: blog-agent
