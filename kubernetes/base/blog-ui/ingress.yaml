apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: blog-ui
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
  - host: dev.petedillo.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: blog-ui
            port:
              number: 80
      # Note: API routes (/api/*) and admin routes (/admin/*, /manage/*) are handled by blog-api ingress
      # NGINX ingress controller matches more specific paths first
