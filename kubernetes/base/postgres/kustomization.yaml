apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- configmap.yaml
- init-configmap.yaml
- pvc.yaml
- deployment.yaml
- service.yaml

# Sealed secrets are handled at overlay level
# Base doesn't include secrets to avoid duplicating across environments